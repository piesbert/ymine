SET(INCLUDE_DIRS
    ${CMAKE_CURRENT_LIST_DIR}/core
)

INCLUDE_DIRECTORIES(${INCLUDE_DIRS})

SET(SOURCE_FILES_CORE
    core/Main.cpp
)

SET(SOURCE_FILES
    ${SOURCE_FILES_CORE}
)

ADD_EXECUTABLE(ymine ${SOURCE_FILES})